import { createTrackReferenceTypeBox } from "./helpers.js";

export default createTrackReferenceTypeBox(
  "Subtitle Track Reference Type Box",
  "Lists tracks referenced by the containing subtitle, timed text or overlay track.",
);
